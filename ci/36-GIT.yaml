 name: test
 channels:
   - conda-forge
 dependencies:
   - python=3.6
   # required
   - geopandas
   - matplotlib
   - numpy>=1.3
   - pandas
   - pygeos
   - scipy>=0.11
   # testing/formatting
   - codecov
   - pytest
   - pytest-cov
   # fetch libpysal master from github
   - pip
   - pip:
     - git+https://github.com/pysal/libpysal.git@master
