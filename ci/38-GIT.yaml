 name: test
 channels:
   - conda-forge
 dependencies:
   - python=3.8
   # required
   - geopandas
   - matplotlib
   - numpy>=1.3
   - pandas
   - pygeos
   - scikit-learn
   - scipy>=0.11
   # testing/formatting
   - codecov
   - pytest
   - pytest-cov
   # fetch packages with pip
   - pip
   - pip:
     - git+https://github.com/pysal/libpysal.git@master
     - opencv-contrib-python>=4.2.0
