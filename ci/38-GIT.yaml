 name: test
 channels:
   - conda-forge
 dependencies:
   - python=3.8
   # required
   - numpy>=1.3
   - opencv-contrib-python>=4.2.0
   - pandas
   - scipy>=0.11
   # testing/formatting
   - codecov
   - pytest
   - pytest-cov
   # fetch libpysal master from github
   - pip
   - pip:
     - git+https://github.com/pysal/libpysal.git@master
