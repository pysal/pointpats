 name: test
 channels:
   - conda-forge
 dependencies:
   - python=3.9
   # required
   - geopandas
   - matplotlib
   - numpy
   - pandas
   - pygeos
   - scikit-learn
   - scipy
   # testing/formatting
   - codecov
   - pytest
   - pytest-cov
   # fetch packages with pip
   - pip
   - pip:
     - git+https://github.com/pysal/libpysal.git@master
     - opencv-contrib-python>=4.2.0
