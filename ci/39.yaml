 name: test
 channels:
   - conda-forge
 dependencies:
   - python=3.9
   # required
   - geopandas
   - libpysal
   - matplotlib
   - numpy
   - pandas
   - pygeos
   - scikit-learn
   - scipy
   # testing/formatting
   - codecov
   - pytest
   - pytest-cov
   # fetch packages with pip
   - pip
   - pip:
     - opencv-contrib-python>=4.2.0
